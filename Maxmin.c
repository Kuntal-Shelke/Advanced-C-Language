#include <stdio.h>
int main() {
    int num1, num2, max;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    *max = (*a > *b) ? *a : *b;

    printf("Maximum: %d\n", max);

    return 0;
}
